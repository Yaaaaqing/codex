# 3分钟演示脚本

## 0) 准备
- npm install
- npm run init
- npm run dev
- 打开 http://localhost:3000

## 1) 员工视角（test001）
1. 登录页输入：test001 → 登录
2. 员工工作台：
   - KPI：我的设备数/版本数/待设基线
   - 设备列表（仅本人创建）
3. 打开“演示机床-001”详情：
   - 看当前/基线卡片
   - 上传一个文件（显示自动生成 display_no）
   - 设为基线/当前（时间线出现事件）
   - 新增“检验记录”（时间线出现事件）

验收：看不到 admin/lead01 的设备；接口也取不到。

## 2) 部门负责人视角（lead01）
1. 退出 → 登录 lead01
2. 部门看板（MT）：
   - KPI：设备总数/已上传/已设基线/待设基线
   - 设备列表：能看到 test001 创建的 MT 设备
   - 部门审计：能看到 MT 范围内操作

验收：看不到 HQ（admin）部门数据。

## 3) 管理员视角（admin）
1. 退出 → 登录 admin
2. 用户管理：
   - 选择用户 → 回填 → 修改 role/dept/enabled → 保存（立即生效）
3. 全量审计：可导出 CSV